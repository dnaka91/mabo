/// Basic struct.
struct Sample {
    a: u32 @1,
    b: bool @2,
}

/// Sample enum.
enum Sample {
    One @1,
    Two(u32 @1, u64 @2) @2,
    Three {
        field1: u32 @1,
        field2: bool @2,
    } @3,
}
